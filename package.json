{
  "name": "pilgrims-progress",
  "version": "1.0.0",
  "description": "A Pilgrim's Progress — Modern Interactive Edition",
  "scripts": {
    "cap:init": "npx cap init",
    "cap:android": "npx cap add android",
    "cap:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:android:open": "npx cap open android",
    "cap:ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "capacitor": "^0.5.6"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/ios": "^6.0.0"
  }
}
